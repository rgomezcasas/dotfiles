# MacOS
# ----------------------------------
# Karabiner
/config/macos/karabiner-elements/automatic_backups

# Common
# ----------------------------------
# Compiled zsh
config/shell/**/**/**.zwc
config/shell/**/**/**.zwc.old
*.zsh.old
config/shell/zsh/.zim

# Agents
# ----------------------------------
!CLAUDE.md
# Re-include the .claude dir first: a path can't be unignored while its parent dir is excluded (~/.gitignore_global ignores .claude)
!.claude/
.claude/*
!.claude/settings.json
