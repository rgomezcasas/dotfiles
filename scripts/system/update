#!/usr/bin/env bash

set -euo pipefail

source "$DOTLY_PATH/scripts/core/_main.sh"

##? Update system packages and shell plugins
#?? 1.0.0
##?
##? Usage:
##?   update
docs::parse "$@"

nix flake update --flake "$DOTFILES_PATH/config/nix"
git -C "$DOTFILES_PATH" add config/nix/flake.lock
if ! git -C "$DOTFILES_PATH" diff --cached --quiet config/nix/flake.lock; then
  git -C "$DOTFILES_PATH" commit -m "chore(nix): update flake.lock"
fi
sudo darwin-rebuild switch --flake "$DOTFILES_PATH/config/nix#pro" --impure
nvd diff $(ls -dt /nix/var/nix/profiles/system-*-link | head -n2)
zimfw upgrade
