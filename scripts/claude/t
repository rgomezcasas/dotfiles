#!/usr/bin/env bash

set -euo pipefail

source "$DOTLY_PATH/scripts/core/_main.sh"

##? Translate natural language to a terminal command
#?? 1.0.0
##?
##? Usage:
##?   t <description>...
docs::parse "$@"

dot claude cc \
  --model "haiku" \
  --append-system-prompt "Responde sólo con el comando. No lo metas dentro de backticks ni nada de markdown." \
  -p "Dame un comando de terminal para ${description[*]}"
