#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
  echo "Usage: dot claude f <prompt>"
  echo "Continue the last Claude conversation"
  exit 1
fi

exec "$(dirname "$0")/cc" --continue -p "$*"
