#!/usr/bin/env bash

set -euo pipefail

source "$DOTLY_PATH/scripts/core/_main.sh"

##? Continue the last Claude conversation
#?? 1.0.0
##?
##? Usage:
##?   f <prompt>...
docs::parse "$@"

exec "$(dirname "$0")/cc" --continue -p "${prompt[*]}"
