#!/usr/bin/env bash
set -euo pipefail
source "$DOTFILES_PATH/scripts/claude/_setup"

if [[ $# -eq 0 ]]; then
  echo "Usage: dot claude f <prompt>"
  echo "Continue the last Claude conversation"
  exit 1
fi

run_claude --continue -p "$*"
