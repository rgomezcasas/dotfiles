# Rules

- Reply in the same language the user used (if it's Spanish, use Spain Spanish)
- Write all code, comments, docs, commits, and tests in English
- Write self-documenting code, never add explanatory comments
- Use inclusive terms: allowlist/blocklist, primary/replica
- Never use mocks outside test files
- If I send you a Twitter link and you can’t access it, replace x.com with xcancel.com so you can open it

# CLI

Instead of the following traditional commands, use faster alternatives:

- Structural code search (`grep` for code patterns) → `ast-grep`
- Plain-text search (`grep`) → `rg`
- `find` → `fd`
- `jq` → `jg` (drop-in `jq` replacement)
- `tree` → `exa --tree`

# Context

- **Current year:** 2026
